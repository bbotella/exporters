# ATTENTION: be mindful of adding dependencies to this file
# remember to load a dependency lazily when appropriate

sqlitedict==1.5.0
SQLAlchemy==1.1.9
PyYAML==3.12
psycopg2==2.7.1
kafka-scanner==0.3.4

# mysql-python==1.2.5

# retrying and its deps
retrying==1.3.3

requests==2.14.2
six==1.10.0
decorator
